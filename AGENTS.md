<!-- AGMO:GENERATED:AGENTS-MD -->
<!-- AGMO:GENERATED:START -->
# Agmo Runtime Instructions

This file is generated by `agmo setup`.

It defines the **Agmo runtime contract** for the scope where it was installed:

- `agmo setup --scope project` → applies to this project tree
- `agmo setup --scope user` → applies to the user Codex home scope

Project-specific coding standards, release policy, and domain rules belong in the preserved manual section, not inside the generated contract block.

## Purpose

Agmo provides a Codex-native runtime for:

- native agent installation under `.codex/agents/*.toml`
- native hook wiring through `.codex/hooks.json`
- durable runtime state under `.agmo/state/*`
- optional team/worktree orchestration
- Obsidian-first vault persistence through `agmo vault ...`

Treat this file as the operational contract for how Agmo expects the workspace to run.

## Core principles

- Prefer evidence before claims.
- Prefer the smallest reversible change.
- Use role separation when it improves quality, speed, or safety.
- Treat durable state as more authoritative than terminal or tmux UI state.
- Record reusable decisions in durable knowledge instead of leaving them only in chat context.

## Runtime contract

- Treat `.agmo/state/*` as the authoritative control plane.
- Treat tmux panes as transport/UI, not durable state.
- Prefer worker-specific worktrees for parallel code changes.
- Do not modify another worker's assigned worktree unless the leader/runtime explicitly reassigns ownership.
- If runtime state and terminal state disagree, inspect the durable state first.

Important runtime locations:

- `.agmo/config.json`
- `.agmo/state/sessions/`
- `.agmo/state/workflows/`
- `.agmo/state/team/`
- `.agmo/logs/`
- `.agmo/memory/`

## Native hook contract

Agmo may manage these native hooks:

- `SessionStart`
- `UserPromptSubmit`
- `PreToolUse`
- `PostToolUse`
- `Stop`

Expected behavior:

- `SessionStart` may inject runtime/bootstrap context.
- `UserPromptSubmit` may persist workflow/session routing state.
- `PreToolUse` may record tool activity and flag destructive commands.
- `PostToolUse` may record success/failure state and emit recovery-oriented guidance.
- `Stop` may finalize session/workflow state.

Hook state may be written to:

- `.agmo/state/sessions/<session>.json`
- `.agmo/state/workflows/<session>.json`

Do not assume hook output is the source of truth if the durable state says otherwise.

## Safety contract

- Destructive commands require extra care even when technically available.
- Read the actual tool input/output before deciding the next step.
- Prefer worktree-safe and reversible operations over destructive workspace mutation.
- Do not claim success immediately after running a command; check its result first.
- If a tool run fails, update the plan from the failure evidence instead of repeating the previous plan verbatim.

## Team runtime contract

When team mode/runtime is active:

- task state, mailbox messages, and dispatch requests are durable artifacts
- worker status and heartbeat are part of the runtime contract
- claim/reclaim/rebalance decisions should respect runtime state, not just chat memory

Team state commonly lives under:

- `.agmo/state/team/<team>/config.json`
- `.agmo/state/team/<team>/tasks/`
- `.agmo/state/team/<team>/workers/`
- `.agmo/state/team/<team>/mailbox/`
- `.agmo/state/team/<team>/dispatch/`

Worker expectations:

- stay inside the assigned worktree/scope
- report progress through runtime-supported paths
- do not silently take over another worker's responsibility

Leader expectations:

- keep the brief current
- prefer durable artifacts over ephemeral instructions
- verify before marking work complete

## Verification contract

- Do not declare completion without fresh verification evidence.
- Prefer running build/check/test commands when they exist.
- If verification fails, treat that output as the new source of truth.
- Distinguish verified facts from inference when reporting status.

## Vault and knowledge contract

If a vault root is configured, Agmo can use:

- `agmo vault config ...`
- `agmo vault save ...`
- `agmo vault scaffold ...`
- `agmo vault create ...`

Vault root resolution order:

1. `AGMO_VAULT_ROOT`
2. project `.agmo/config.json`
3. user `~/.agmo/config.json`

Knowledge guidance:

- persist durable plans, implementations, research, and decisions
- prefer structured notes over transient summaries
- keep project knowledge linkable and reusable

## Native role guidance

Agmo native roles typically include:

- `agmo-planner`
- `agmo-executor`
- `agmo-verifier`
- `agmo-wisdom`
- `agmo-architect`
- `agmo-critic`
- `agmo-explore`

Use them according to task shape:

- planning/decomposition → planner
- direct implementation → executor
- evidence review/testing → verifier
- vault/doc/knowledge retrieval → wisdom
- read-only design/tradeoff analysis → agmo-architect
- read-only plan/design challenge → agmo-critic
- fast repo fact gathering → agmo-explore

## Managed file contract

- `agmo setup` owns the generated contract section.
- The manual section is preserved across future setup runs.
- Runtime and worker overlay marker blocks are reserved for future Agmo overlays.
- If an unmanaged `AGENTS.md` already exists, `agmo setup` should not replace it unless explicitly forced.
<!-- AGMO:GENERATED:END -->

<!-- AGMO:RUNTIME:START -->

<!-- AGMO:RUNTIME:END -->

<!-- AGMO:TEAM:WORKER:START -->

<!-- AGMO:TEAM:WORKER:END -->

<!-- AGMO:MANUAL:START -->
<!-- AUTONOMY DIRECTIVE — DO NOT REMOVE -->
YOU ARE AN AUTONOMOUS CODING AGENT. EXECUTE TASKS TO COMPLETION WITHOUT ASKING FOR PERMISSION.
DO NOT STOP TO ASK "SHOULD I PROCEED?" — PROCEED. DO NOT WAIT FOR CONFIRMATION ON OBVIOUS NEXT STEPS.
IF BLOCKED, TRY AN ALTERNATIVE APPROACH. ONLY ASK WHEN TRULY AMBIGUOUS OR DESTRUCTIVE.
USE CODEX NATIVE SUBAGENTS FOR INDEPENDENT PARALLEL SUBTASKS WHEN THAT IMPROVES THROUGHPUT.
<!-- END AUTONOMY DIRECTIVE -->

## Workspace extension area

This repository now treats **Agmo** as the primary runtime contract.

Legacy Agmo surfaces should be retired instead of preserved as compatibility shims unless a file explicitly says they remain operationally required.

## Working agreements

- Write a cleanup plan before modifying code for cleanup/refactor/deslop work.
- Lock existing behavior with regression tests before cleanup edits when behavior is not already protected.
- Prefer deletion over addition.
- Reuse existing utils and patterns before introducing new abstractions.
- No new dependencies without explicit request.
- Keep diffs small, reviewable, and reversible.
- Run lint, typecheck, tests, and static analysis after changes.
- Final reports must include changed files, simplifications made, and remaining risks.

## Preferred workflow surface

Use the public Agmo workflow chain by default:

`brainstorming -> plan -> plan-review -> execute -> team`

Guidance:

- use `brainstorming` for ideation and tradeoffs (`design` is compatibility alias only)
- use `plan` for execution-ready planning
- use `plan-review` to challenge or approve a plan
- use `execute` for implementation in the current session
- use `team` only when durable tmux workers materially improve delivery or verification
- use `wisdom` for durable knowledge/doc synthesis
- use `verify` for tests, evidence gathering, and completion review

## Delegation and execution

- Default posture: work directly.
- Delegate only when it materially improves quality, speed, or safety.
- For substantive code changes, `executor` is the default implementation role.
- Use native subagents for bounded parallel work; the leader owns integration and final verification.
- Reserve durable team runtime for stateful tmux worker lanes rather than short-lived in-process help.

## Verification and reporting

- Verify before claiming completion.
- If new logs, test failures, or runtime evidence appear, treat them as the new source of truth.
- Distinguish evidence from inference in status reports.
- Continue through clear, low-risk, reversible next steps automatically.

## Lore Commit Protocol

Every commit message must follow the Lore protocol — structured decision records using native git trailers.

```text
<intent line: why the change was made, not what changed>

<body: narrative context — constraints, approach rationale>

Constraint: <external constraint that shaped the decision>
Rejected: <alternative considered> | <reason for rejection>
Confidence: <low|medium|high>
Scope-risk: <narrow|moderate|broad>
Directive: <forward-looking warning for future modifiers>
Tested: <what was verified (unit, integration, manual)>
Not-tested: <known gaps in verification>
```

Rules:

1. The first line explains **why**.
2. Use trailers when they add real future value.
3. Record rejected alternatives so future work does not re-open dead ends unnecessarily.
4. Be honest about verification gaps.

## Migration note

- Keep runtime guidance Agmo-first; remove stale legacy artifacts instead of reintroducing compatibility-only surfaces.
<!-- AGMO:MANUAL:END -->
