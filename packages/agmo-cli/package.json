{
  "name": "agmo",
  "version": "0.1.0",
  "description": "Agmo Codex runtime CLI",
  "type": "module",
  "bin": {
    "agmo": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist && cp -R src/templates dist/templates && node dist/cli/index.js config vault-autosave generate-docs --output dist/templates/vault-autosave/SCHEMA.md",
    "check": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}
